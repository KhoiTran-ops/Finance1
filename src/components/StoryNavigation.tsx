import {scenes} from '../scenes/story';
export default function StoryNavigation({index,onChange}:{index:number,onChange:(n:number)=>void}){return <footer className="story-nav"><div className="progress-label"><b>{String(index+1).padStart(2,'0')}</b><span>/ {scenes.length}</span></div><nav aria-label="Các cảnh câu chuyện">{scenes.map((s,i)=><button key={s.id} title={s.label} aria-label={`${i+1}. ${s.label}`} aria-current={i===index?'step':undefined} className={i===index?'active':i<index?'visited':''} onClick={()=>onChange(i)}><span/>{i===index&&<em>{s.label}</em>}</button>)}</nav><div className="nav-actions"><button aria-label="Cảnh trước" disabled={!index} onClick={()=>onChange(index-1)}>←</button><button className="next" onClick={()=>onChange(index===9?0:index+1)}>{index===9?'Về đầu':'Next'} <span>→</span></button></div></footer>}
