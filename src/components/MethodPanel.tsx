import {meta} from '../data/model';
export default function MethodPanel(){return <div className="method-grid">{[['Trạng thái','DEMO — chưa có dữ liệu ADB'],['Nguồn chỉ số',meta.source],['Đơn vị & kỳ',`${meta.unit}; ${meta.period}`],['Cách tính','Chỉ số = giá trị năm t / giá trị năm 2019 × 100. Chỉ áp dụng khi cùng định nghĩa và năm gốc hợp lệ.'],['Dữ liệu thiếu','Không nội suy mặc định. Khi tích hợp ADB, dùng null và hiển thị khoảng trống; không đổi dữ liệu thiếu thành 0.'],['Vai trò Singapore','Benchmark riêng; không dùng để tạo thứ hạng bốn quốc gia trọng tâm.'],['Trước khi công bố','Chốt mã chuỗi ADB, đơn vị, giá hiện hành/cố định, nguồn, ngày tải và ghi chú điều chỉnh. Kiểm tra khả năng so sánh.'],['Bản đồ','Natural Earth qua datasets/geo-countries (Public Domain). Hình học khái quát, không thể hiện quan điểm về ranh giới.']].map(([title,text])=><section key={title}><h3>{title}</h3><p>{text}</p></section>)}</div>}
